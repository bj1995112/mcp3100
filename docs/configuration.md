# MCP3100 配置说明

## 必需

`STD_AUTH_TOKEN`：MCP3100 的 Bearer Token。生产/远程连接时必须使用随机高强度值。

`STD_PORT`：默认 `3100`。

## 第一次安装

安装器会自动创建 `.env`（如果不存在）。ChatGPT/Tunnel 数据不要求第一次填写。

## 不进入 Git

- `.env`
- API Key
- Bearer Token
- Tunnel 凭据
- 日志
- node_modules
